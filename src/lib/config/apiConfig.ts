type ApiEndPoints = {
    wallets: {
        add: string
    }
}

export const BASE_API_URL = "https://tccc-be.vercel.app/"

export const API_ENDPOINTS: ApiEndPoints = {
    wallets: {
        add: "/wallet/add"
    }
}